import SettingsScreen from './settings/SettingsScreen'

function App(): React.JSX.Element {
  // Main window screen: the VEYRA settings screen (plan step 7).
  return <SettingsScreen />
}

export default App
