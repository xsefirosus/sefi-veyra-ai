---
status: approved
reason: gate-red
date: 2026-08-19
source: software-engineer dispatch (step 3)
---

# gate.sh red in app/ — Python branch false-triggers on app/node_modules/**/*.py

## Gate evidence (raw)

Gate command (run from `app/`, the npm-branch location for this slice):
`/c/Users/Mary%20Rose/.config/opencode/scripts/gate.sh` via git bash, workdir `D:/Projects/Sefi-Veyra-AI/app`.

```
ok: npm-lint
ok: npm-typecheck
ok: ruff
FAIL: pytest (exit 5)
no error-keyword lines; last 20 lines of output:

no tests ran in 0.04s
full log: .worktrees/logs/20260819-220545_pytest.log
gate: FAILED (exit 5)
```

App npm scripts present: `lint` (eslint --cache .) — PASS; `typecheck`
(typecheck:node && typecheck:web) — PASS; `test` — does not exist yet (step 4 adds vitest).

## Root cause — gate.sh Python detector does not exclude node_modules

gate.sh line 92: `find . -name '*.py' -not -path './.git/*' -not -path './.worktrees/*'`
matches any `.py` anywhere. Step 3's `npm install` pulled dependency `.py` files into
`app/node_modules` (e.g. `flatted/python/flatted.py`, `node-gyp/gyp/*.py`), so the Python
branch triggers even in app/ and runs `pytest -q` with zero tests to collect → exit 5.
The Shell branch (line 119) already excludes `./node_modules/*`; the Python detector does not.

## Not fixable within this slice

The slice builds an Electron app; the npm branch (lint/typecheck) passes. The red comes
from harness gate.sh detection, which this slice must not edit (shared tooling outside the
repo). pytest exit 5 is a false trigger, not a slice defect — `npm run build` and
`npm run typecheck` exit 0, committed as 0ef7a66.

Human decision needed: add `-not -path './node_modules/*'` to gate.sh's Python find
(line 92), or accept a documented exception for npm-installed node_modules.

## State

- Step 3 checkbox marked `- [x]`, committed fbccf53.
- `state/build-baseline.log` committed a9db78d.
- Slice work (npm install 590 pkgs, build exit 0) committed 0ef7a66; kept as-is.

## Approval (2026-08-19)

Human decision (engineering-manager, recorded on user's behalf): patch the shared
install's `gate.sh` — add `-not -path './node_modules/*'` to the Python `find` on line 92,
mirroring the Shell branch's existing exclusion. Applied to BOTH copies:

- `C:/Users/Mary Rose/.config/opencode/scripts/gate.sh` (active install) — MD5
  `B9BD043BB031FD744E6A195A6C59A785`
- `C:/Users/Mary Rose/.cache/opencode/sefi-agents/plugins/sefi-core/scripts/gate.sh`
  (plugin cache) — MD5 `FBC00677973B1929593EB091B35F4237`

Pre-edit the two files differed only in line endings (active LF, cache CRLF); no content
divergence was clobbered. Only the Python find exclusion list changed; nothing else.

Verification (run from `D:/Projects/Sefi-Veyra-AI/app` via git bash, active-install copy):
exit code 0 — `ok: npm-lint`, `ok: npm-typecheck`, `gate: PASSED (2 checks)`, and zero
pytest lines in the output (Python branch no longer triggers; no `.py` outside
node_modules). Fix confirmed by execution; gate.sh edits are outside the repo (shared
install) and were not committed here.